priorities = [0, 5, 2, 10, 3]

for num in priorities:
    if num < 5:
        print(num)

#0
#2
#3

# Q1: "medium"
# Q2: the assignment operator is being used instead of the comparison == operator. 
# Q3: no it doesn't print. the and expression evaluates to false and the code block is skipped.
""" Q4: 
for num in ip_counts:
    if num > 0:
        print(num)
"""